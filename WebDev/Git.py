# cherry-pick command in git
"""git cherry-pick is used to apply a specific commit from one branch to 
another without merging the entire branch."""

# git merge vs rebase
"""Git merge combines branches and creates a merge commit while preserving history.
Git rebase moves a branch on top of another, rewriting history to create a clean linear timeline.""" 