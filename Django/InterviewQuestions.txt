*What does REST mean?
-> Representational State Tansfer, its an architecture style to write api to communicate of F-End to B-End using HTTP.
Server is stateless.
REST principles:
Resource-based
HTTP Methods
Stateless
Representation
Client–Server separation

*What are RESTful Api?
-> APIs that follow REST principles

*⁠What is stateless & stateful api?
->Stateless-API: request is treated independent transaction, no inherent memory of previous requests.
Stateful-API: server maintain/retain info of client session/past interaction.

*⁠RESTful APIs are stateful or stateless? ->RESTful APIs must be stateless
*URLs in django-> mapping URLs to view functions/class
*Models in django-> structure and behavior of db tables
*⁠Views in django-> handle the business logic and data display
*⁠Django project architecture-> Model-View-Template 
*⁠ORM in django-> Pythonic way to interact with relational db without writing raw SQL code
*⁠How to obtain SQL query from queryset-> str(my_queryset.query)

*⁠How django determines whether migrations are already applied?
->recording each successful migration in a dedicated db table called django_migrations

*Q objects in Django?
-> Q objects let you build flexible filters by combining conditions with OR, AND, and NOT, 
so you can create more complex WHERE parts in the SQL queries that Django generates.


*Difference b/w Django vs Fast API vs Flask
->
Django: Full Web Framework includes admin, ORM, auth, template.Uses WSGI
FastAPI: API Framework,high-performance, async bydefault,used for microservice. Uses ASGI

*⁠Which to use Django or Fast API?
->
Django: Business-heavy projects
FastAPI: High Performance or microservice architecture, High-traffic public APIs

*⁠What is CSRF?
->
Security Attact where a malicious site forces a logged-in user to perform 
unintended actions on a trustes site using their authenticated session
Example:
User logs into a trusted site (e.g. bank.com)
Session cookie is stored in the browser
User visits a malicious website
Malicious site sends a hidden request to bank.com
Browser automatically attaches cookies
Server thinks the request is genuine.

CSRF in Django: Django enables CSRF protection by default.(CsrfViewMiddleware)


*Where redis stores the data? -> RAM

*⁠How do you improve API response?
->
optimizing database queries: avoid N+1 query
caching: by redis
reducing payload size: by pagination
indexing: on foreign keys, filter/search fields
Async APIs
background tasks
Scaling infra: by multiple instance, loadbalancer


⁠how do you handle complex queries in django
⁠Explain select_related & prefetch_related
⁠what is the meaning of DEBUG = TRUE in django.Is it set to True or False in prod environment?
Have you faced memory leak issue and how did u solve 
How to secure api in django
Put vs patch 
Difference between gunicon and uvicon
How to handle exception in django
Components of jwt token
How do u handle refresh token
What is OAuth

